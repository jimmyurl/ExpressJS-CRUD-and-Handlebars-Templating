<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <link rel="stylesheet" href="https://maxcdn.bootstrapcdn.com/bootstrap/4.5.2/css/bootstrap.min.css">
    <title>Superstars</title>
</head>
<body class="inner">
    <div class="container mt-5">
        <h1 class="text-center mb-3">{{title}}</h1>
        <form action="/api/members" method="POST" class="row mb-3">
            <div class="col-md-6">
                <label for="username" class="form-label">Player</label>
                <input type="text" id="username" name="username" class="form-control">
            </div>
            <div class="col-md-6">
                <label for="team" class="form-label">Team</label>
                <input type="text" id="team" name="team" class="form-control">
            </div>
            <div class="col-md-6">
                <label for="inputCity" class="form-label">City</label>
                <input type="text" id="inputCity" class="form-control">
            </div>
            <div class="col-md-6">
                <label for="inputState" class="form-label">State</label>
                <select id="inputState" class="form-select">
                    <option selected>Choose...</option>
                    <option>...</option>
                </select>
            </div>
            <div class="col-12 text-center mt-4">
                <button type="submit" class="btn btn-primary">Add Player</button>
            </div>
        </form>
        <h4>NBA Superstars</h4>
        <ul class="list-group">
            {{#each members}}
            <li class="list-group-item">{{this.team}}: {{this.username}}</li>
            {{/each}}
        </ul>
        <a href="/api/members" class="btn btn-light mt-4">Check API</a>
    </div>
</body>
</html>
